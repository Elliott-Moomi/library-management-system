spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/library_management_system
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  flyway:
    enabled: true

server:
  error:
    include-message:
---
spring.profiles.active=dev
#      spring:
#        datasource:
#          url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:library_management_system}
#          username: ${DB_USERNAME:postgres}
#          password: ${DB_PASSWORD:root}
#          driver-class-name: org.postgresql.Driver
#        jpa:
#          generate-ddl: true
#          hibernate:
#            ddl-auto: validate
#          show-sql: true
#          properties:
#            hibernate:
#              dialect: org.hibernate.dialect.PostgreSQLDialect
#              format_sql: true
#
#      server:
#        error:
#          include-message: always